<Window x:Class="DPGI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DPGI"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="TextEditor" MinHeight="250" Height="450" MinWidth="300" Width="600">
    <Window.Resources>
        <LinearGradientBrush x:Key="LinearPanelBrush">
            <GradientStop Color="Yellow" Offset="0"/>
            <GradientStop Color="Aqua" Offset="0.5"/>
            <GradientStop Color="Yellow" Offset="0.8"/>
        </LinearGradientBrush>

        <Style TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="White"/>
            <Setter Property="Margin" Value="5"/>
        </Style>
    </Window.Resources>

    <Grid ShowGridLines="False">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" BorderBrush="#FFCBBEBE" BorderThickness="2" CornerRadius="10">
            <StackPanel Orientation="Horizontal" Background="{StaticResource LinearPanelBrush}">
                <Button Width="50" Height="50" Command="Open">
                    <Image ToolTip="Open" Source="images/Open.png"/>
                </Button>
                <!-- Изменён: Command="Delete" на Command="Close", чтобы привязка сработала -->
                <Button Width="50" Height="50" Command="Close">
                    <Image ToolTip="Clear" Source="images/Clear.png"/>
                </Button>
                <Button Width="50" Height="50" Command="Copy" CommandTarget="{Binding ElementName=MainTextBox}">
                    <Image ToolTip="Copy" Source="images/Copy.png" />
                </Button>
                <Button Width="50" Height="50" Command="Paste" CommandTarget="{Binding ElementName=MainTextBox}">
                    <Image ToolTip="Paste" Source="images/Paste.png"/>
                </Button>
            </StackPanel>
        </Border>

        <Border Grid.Row="1" BorderBrush="#FFCBBEBE" BorderThickness="2" CornerRadius="10">
            <Grid Background="{StaticResource LinearPanelBrush}">
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBox x:Name="MainTextBox"
                         Grid.Row="0"
                         Margin="10"
                         AcceptsReturn="True"
                         TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Visible"
                         FontSize="{Binding ElementName=FontSlider, Path=Value, UpdateSourceTrigger=PropertyChanged}"/>

                <DockPanel Grid.Row="1" Margin="10 5 10 10">
                    <Label>Розмір шрифту:</Label>
                    <Slider Margin="4" MinWidth="100" Name="FontSlider" Maximum="100" Minimum="12"/>
                    <Button Width="30" DockPanel.Dock="Bottom" Style="{x:Null}"
                            Content="OK" ToolTip="Save"
                            Command="Save"/>
                </DockPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
